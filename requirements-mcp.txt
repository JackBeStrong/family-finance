# Family Finance MCP Server Dependencies

# MCP SDK for Python (1.2+ for Streamable HTTP transport)
mcp[cli]>=1.2.0

# Database ORM with connection pooling
sqlalchemy>=2.0.0

# PostgreSQL driver (used by SQLAlchemy)
psycopg2-binary>=2.9.0

# YAML parsing for context store
pyyaml>=6.0.0
